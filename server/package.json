{
  "name": "studio-management-server",
  "version": "1.0.0",
  "description": "Backend API for Studio Management System with admin, payments, and image uploads.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client-install": "cd ../client && npm install",
    "client-build": "cd ../client && npm run build",
    "heroku-postbuild": "cd ../client && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cloudinary": "^1.34.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongodb-memory-server": "^8.11.1",
    "mongoose": "^9.6.1",
    "multer": "^1.4.5-lts.1",
    "openai": "^4.11.0",
    "stripe": "^12.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
